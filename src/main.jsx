import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './NewArService.jsx'; // Certifique-se de que o nome do ficheiro coincide
import './index.css'; // Certifique-se de que este ficheiro existe

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
